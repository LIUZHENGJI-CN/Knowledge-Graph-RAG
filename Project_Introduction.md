本项目旨在复现并构建一个面向学术论文的知识图谱增强问答系统。项目数据主要来源于 arXiv 开放论文平台，重点关注 `q-bio` 定量生物学相关论文，包括 Biomolecules、Genomics、Molecular Networks、Cell Behavior、Quantitative Methods 等多个子方向，共约2000篇。这类论文兼具生物学问题和计算建模、网络分析、机器学习等方法特征，适合用于构建学术知识图谱与语义问答系统。

项目首先从 arXiv 获取论文的基础元数据，包括标题、arXiv ID、发表时间、DOI、PDF 链接、作者和学科分类等信息。随后利用大语言模型从论文标题和摘要中抽取研究背景、核心方法和关键词，将原始论文数据转化为更结构化的知识单元。

在图谱构建阶段，系统使用 Neo4j 存储知识图谱。论文被建模为 `Paper` 节点，作者、关键词、学科分类、机构和发表渠道分别被建模为 `Author`、`Keyword`、`Category`、`Institution`、`Journal/Conference` 节点。系统通过 `AUTHORED_BY`、`HAS_KEYWORD`、`BELONGS_TO`、`AFFILIATED_WITH`、`PUBLISHED_IN` 等关系连接这些实体，从而形成可查询、可分析的学术知识网络。

为了增强图谱信息，项目进一步通过 arXiv、Crossref、OpenAlex、PDF 首页文本和机构域名映射等方式补充摘要、全文、作者机构等信息。对于规则方法难以识别的机构信息，系统会借助大语言模型进行补全。

至此，学术知识图谱的主体构建完成。接下来，系统在 Neo4j 图谱基础上引入语义向量检索和 RAG 问答能力，使其同时支持结构化关系查询和论文内容理解。

在语义检索方面，项目使用 BGE-M3 模型将论文标题、摘要、背景、方法和关键词转换为向量，并写入 Neo4j 向量索引。这样系统既能进行传统图谱结构查询，也能根据用户问题进行语义相似论文检索。

核心问答层结合了 Text2Cypher 和 Fast RAG 两种方式。对于作者合作、机构研究方向、论文统计等结构化问题，系统会将自然语言转换为 Cypher 查询 Neo4j；对于研究主题解释、方法总结等语义问题，系统会通过向量召回、重排序和大语言模型生成回答。前端采用 Streamlit 构建聊天式界面，用户可以直接输入问题，系统自动选择合适的查询路线并返回答案、参考论文和图谱查询结果。

总体来说，本项目实现了从 arXiv 论文采集、知识抽取、Neo4j 图谱构建、语义向量化到智能问答展示的一整套流程，目标是帮助用户更高效地理解定量生物学领域中的论文内容、作者关系、研究机构和主题分布。
